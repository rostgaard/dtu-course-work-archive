let rng: 100; len:10;  a[!len]: 1 ; sum : 0
in a := randomArray(rng, len);
   call printArray(!a);
   sum := sumArray(!a);
   print "Sum:";
   print toString(!sum);
   if <>(a.length, 10) then
      print "(failed assertion!)"
   else
     print "(ok)"
   fi
end
