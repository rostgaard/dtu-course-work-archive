package Railval.Trace is

   procedure Debug (Context : in String;
                    Message : in String);

   procedure Error (Context : in String;
                    Message : in String);
end Railval.Trace;
